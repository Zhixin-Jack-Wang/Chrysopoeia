import { Modal, Button } from "react-bootstrap";
import React from "react";
import styled from "styled-components";
const Chat = props => {
  return (
    <Wrapper>
      <Modal
        {...props}
        size="lg"
        aria-labelledby="contained-modal-title-vcenter"
        centered
      >
        <Modal.Header closeButton className="m-head" id="m-head">
          <Modal.Title id="contained-modal-title-vcenter">
            Modal heading
          </Modal.Title>
        </Modal.Header>
        <Modal.Body className="m-body" id="m-body" />
        <Modal.Footer>
          <Button onClick={props.onHide}>Close</Button>
        </Modal.Footer>
      </Modal>
    </Wrapper>
  );
};

export default Chat;

const Wrapper = styled.section`
  .modal-header {
    border: none;
  }
  .modal-body {
    border: none;
  }
  .m-head {
    border: none;
  }
  #m-head {
    border: none;
  }
  #m-body {
    border: none;
  }
`;
